# Wiring the runner

### Registration

```csharp
builder.Services.AddAzureDeploymentServices();
```

<br/>

### Execution

```csharp
var stackArgs = new InlineProgramArgs(options.ProjectName, options.StackName, PulumiFn.Create(async () =>
{
    await deploymentProcessor.ProcessDeploymentAsync(cancellationToken);
}));

var stack = await LocalWorkspace.CreateOrSelectStackAsync(stackArgs, cancellationToken);
```

<br/>

- one line registers the standard Azure-specific behavior
- the processor is executed through a normal Pulumi Automation stack
- the model stays separate from the command that runs it

<!--
At this point the setup is intentionally small.

The first block shows the setup point.
One registration brings in the standard Azure-specific deployment behavior.

The second block shows the execution point.
The processor is wrapped as a normal Pulumi Automation inline program, and then the stack is created or selected.

So the important idea is that this is not a special deployment runtime.
It is a normal .NET application plus Pulumi Automation.
-->

---

### Pulumi Preview

<div class="run-single mt-4">

```csharp
await stack.PreviewAsync(cancellationToken);
```

</div>

<div class="run-log mt-6">

<pre><span style="color:#ff00ff"><strong>Previewing update (prod-sandbox):</strong></span>

 <strong><span style="color:#00d700">+ </span></strong> pulumi:pulumi:Stack LiveArch.Deployment-prod-sandbox <strong><span style="color:#00d700">create</span></strong>
<span style="color:#d7af00">@ previewing update.........</span>
 <strong><span style="color:#00d700">+ </span></strong> azure-native:storage:StorageAccount testSa <strong><span style="color:#00d700">create</span></strong>
 <strong><span style="color:#00d700">+ </span></strong> azure-native:managedidentity:UserAssignedIdentity testMi <strong><span style="color:#00d700">create</span></strong>
 <strong><span style="color:#00d700">+ </span></strong> azure-native:authorization:RoleAssignment Contribute <strong><span style="color:#00d700">create</span></strong>
 <span style="color:#00d700">+ </span> pulumi:pulumi:Stack LiveArch.Deployment-prod-sandbox <span style="color:#00d700">create</span>

<span style="color:#ff00ff"><strong>Resources:</strong></span>
    <span style="color:#00d700">+ 4 to create</span></pre>

</div>

<!--
This is the preview step.

Nothing is created yet.
But Pulumi can already show the concrete deployment plan that came from the model.

So even before apply, I can validate what the engine and Pulumi together intend to do.
-->

---

### Pulumi Up

<div class="run-single mt-4">

```csharp
await stack.UpAsync(cancellationToken);
```

</div>

<div class="run-log mt-6">

<pre><span style="color:#ff00ff"><strong>Updating (prod-sandbox):</strong></span>

<span style="color:#d7af00">@ updating....</span>
 <strong><span style="color:#00d700">+ </span></strong> pulumi:pulumi:Stack LiveArch.Deployment-prod-sandbox <strong><span style="color:#00d700">creating (0s)</span></strong>
<span style="color:#d7af00">@ updating...........</span>
 <strong><span style="color:#00d700">+ </span></strong> azure-native:storage:StorageAccount testSa <strong><span style="color:#00d700">creating (0s)</span></strong>
 <strong><span style="color:#00d700">+ </span></strong> azure-native:managedidentity:UserAssignedIdentity testMi <strong><span style="color:#00d700">creating (0s)</span></strong>
<span style="color:#d7af00">@ updating.....</span>
 <span style="color:#00d700">+ </span> azure-native:managedidentity:UserAssignedIdentity testMi <span style="color:#00d700">created (1s)</span>
<span style="color:#d7af00">@ updating.....................</span>
 <span style="color:#00d700">+ </span> azure-native:storage:StorageAccount testSa <span style="color:#00d700">created (20s)</span>
 <strong><span style="color:#00d700">+ </span></strong> azure-native:authorization:RoleAssignment Contribute <strong><span style="color:#00d700">creating (0s)</span></strong>
<span style="color:#d7af00">@ updating......</span>
 <span style="color:#00d700">+ </span> azure-native:authorization:RoleAssignment Contribute <span style="color:#00d700">created (3s)</span>
 <span style="color:#00d700">+ </span> pulumi:pulumi:Stack LiveArch.Deployment-prod-sandbox <span style="color:#00d700">created (34s)</span>
<span style="color:#ff00ff"><strong>Resources:</strong></span>
    <span style="color:#00d700">+ 4 created</span>

<span style="color:#ff00ff"><strong>Duration:</strong></span> 36s</pre>

</div>

<!--
This is the real apply step.

Here the same plan is actually executed against Azure.
The log makes the ordering visible: first the identity and storage account, then the role assignment that depends on them.

So this is no longer only a modeled intent.
It is a real deployment run.
-->

---

### Pulumi Destroy

<div class="run-single mt-4">

```csharp
await stack.DestroyAsync(cancellationToken);
```

</div>

<div class="run-log mt-6">

<pre><span style="color:#ff00ff"><strong>Destroying (prod-sandbox):</strong></span>

<span style="color:#d7af00">@ destroying.....</span>
 <strong><span style="color:#d70000">- </span></strong> azure-native:authorization:RoleAssignment Contribute <strong><span style="color:#d70000">deleting (0s)</span></strong>
<span style="color:#d7af00">@ destroying...........</span>
 <span style="color:#d70000">- </span> azure-native:authorization:RoleAssignment Contribute <span style="color:#d70000">deleted (10s)</span>
 <strong><span style="color:#d70000">- </span></strong> azure-native:managedidentity:UserAssignedIdentity testMi <strong><span style="color:#d70000">deleting (0s)</span></strong>
 <strong><span style="color:#d70000">- </span></strong> azure-native:storage:StorageAccount testSa <strong><span style="color:#d70000">deleting (0s)</span></strong>
<span style="color:#d7af00">@ destroying....</span>
 <span style="color:#d70000">- </span> azure-native:storage:StorageAccount testSa <span style="color:#d70000">deleted (2s)</span>
<span style="color:#d7af00">@ destroying....</span>
 <span style="color:#d70000">- </span> azure-native:managedidentity:UserAssignedIdentity testMi <span style="color:#d70000">deleted (4s)</span>
<span style="color:#d7af00">@ destroying....</span>
 <strong><span style="color:#d70000">- </span></strong> pulumi:pulumi:Stack LiveArch.Deployment-prod-sandbox <strong><span style="color:#d70000">deleting (0s)</span></strong>
 <span style="color:#d70000">- </span> pulumi:pulumi:Stack LiveArch.Deployment-prod-sandbox <span style="color:#d70000">deleted (0.04s)</span>
   pulumi:pulumi:Stack LiveArch.Deployment-prod-sandbox

<span style="color:#ff00ff"><strong>Resources:</strong></span>
    <span style="color:#d70000">- 4 deleted</span>

<span style="color:#ff00ff"><strong>Duration:</strong></span> 17s</pre>

</div>

<!--
And finally, the same modeled deployment can be torn down again.

The destroy log also makes the dependency order visible in reverse.
The role assignment goes first, then the resources it depends on.

So the model is participating not only in creation, but in the whole lifecycle.
-->